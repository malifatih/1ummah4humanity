import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: '1Ummah | Connect Globally',
  description: 'The open source social platform for the global community. Democratic, transparent, safe, and rewarding.',
  keywords: ['social media', 'open source', 'decentralized', 'community', '1ummah'],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
